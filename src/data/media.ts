// Swap this for the real proposal video — either a local file placed at
// /public/videos/proposal.mp4, or a full external URL if it ends up
// hosted elsewhere (Vimeo/Cloudinary direct file link, etc.). Until a real
// file exists here, the video frame gracefully falls back to a labeled
// placeholder instead of a broken player.
export const proposalVideoSrc = '/videos/proposal.mp4'

// Shown before playback starts, and as a fallback poster if the video
// itself fails to load.
export const proposalVideoPoster = '/images/proposal-poster.jpg'

// The real first-message screenshot for "The First Conversation" — the
// cropped version, showing just the date badge, encryption notice, and
// opening messages, rather than the full longer screenshot.
export const firstMessageScreenshot = '/images/first-message-crop.jpg'

// The photo shown in "THE QUESTION" story beat — the ring and cake.
export const proposalPhoto = '/images/proposal-ring-cake.jpg'
